

//Contar el numero de carácteres de una cadena incluyendo espacios
let contadorLetra = 'montesino'
console.log(contadorLetra.length)

//para cortar la cadena utilizando slice
let texto = 'monterroza'
let resultado = texto.slice(6,9)
console.log(resultado)

//para remplazar una palabra de una cadena utilizando (replace)
let text = 'llegaré un poco temprano'
let result = text.replace('temprano','tarde')
console.log(result)

//cambiar toda la cadena a texto utilizando .toUpperCase() y para minuscular .toLowerCase()
let textom = 'Esto es una cadena'
let devuelve = textom.toUpperCase()
console.log(devuelve)

